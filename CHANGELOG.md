# Ironsight Backend — Changelog

## 2026-06-09

- **fix** live-stream: switch gohlslib variant `MuxerVariantLowLatency` -> `MuxerVariantFMP4` in `internal/streaming/livehls.go` — even after PR #42 (`lowLatencyMode: false` on hls.js) and PR #44 (vendored mediacommon `array_completeness=1`), the LL-HLS scaffolding in the served manifest (`#EXT-X-PART`, `#EXT-X-PART-INF`, `#EXT-X-SERVER-CONTROL CAN-BLOCK-RELOAD`, `#EXT-X-PRELOAD-HINT`, mixed 5 s + 1 s part durations) still tripped hls.js parser paths and dumped the entire manifest as a fatal error on the test user. FMP4 variant emits a plain HLS playlist with only `#EXTINF` segments + `#EXT-X-MAP` init — no part tags at all, nothing for hls.js to choke on. Latency goes ~6-10 s -> ~12-18 s, fine for security trailer monitoring. The PR #39 "0-byte fMP4" theory was misdiagnosed: 0-byte responses happen for ANY rotated-out segment regardless of variant; verified via direct curl that fresh segments serve ~157 KB. `PartMinDuration` field + `liveHLSPartMinDuration` constant removed (only meaningful for LL variant).

## 2026-06-08

- **fix** live-stream: vendor `github.com/bluenviron/mediacommon/v2` at `v2.8.3` with `array_completeness=1` patch — upstream emits `hvc1` sample-entry but leaves `array_completeness=0` on VPS/SPS/PPS NALU arrays, violating ISO/IEC 14496-15 §8.4.1.1.1 and causing Chrome MSE `bufferAppendError / MANIFEST_INCOMPATIBLE_CODECS_ERROR`; vendored copy in `internal/vendored/mediacommon` with `Completeness: true` on all three `HEVCNaluArray` entries; `go.mod` replace directive wires it in; upstream PR filed separately; see `internal/streaming/livehls_h265_init_test.go` for byte-level verification of the hvcC box.

- **fix** live-stream: surface real hls.js error on screen instead of generic "Stream unavailable" — when fatal hls.js error fires we now include the `type/details` and HTTP status code (if any) inline in the error box so ops can diagnose without opening F12; mint-failure path shows the underlying exception message. Catches the common "Chrome installed without HEVC hardware decoder" case (`mediaError/manifestIncompatibleCodecsError` or `mediaError/bufferIncompatibleCodecsError`) cleanly. No new behavior — pure diagnostic improvement.
- **fix** live-stream player: disable `lowLatencyMode` in hls.js for both `VideoPlayer.tsx` (live feed) and `shared/HLSVideoPlayer.tsx` (re-used by PPE zone editor + other LL surfaces) — even after bumping `PartMinDuration` to 5s, gohlslib's `partTargetDuration` is the running max of observed parts in the segment so any cellular jitter still bumps `#EXT-X-PART-INF:PART-TARGET` from one manifest fetch to the next; hls.js with `lowLatencyMode: true` treats this as a fatal LL-HLS spec violation and drops the stream ("flash then disappear"); classic HLS mode ignores PART tags entirely and plays whole 6s segments instead. Latency ~6-10s but the stream stays alive. Server-side LL-HLS muxer config unchanged (still emits full segments alongside parts, so non-LL clients work transparently).
- **fix** live-stream: bump `liveHLSPartMinDuration` 1s→5s and `liveHLSSegmentMinDuration` 2s→6s — H.265 cellular jitter (~150ms) was ~15% of the 1s part target, triggering gohlslib's LL-HLS spec-violation warning on every cycle and killing browser players; at 5s it's ~3% (noise); latency tradeoff ~10-15s accepted for security trailer use.
- **fix** mediamtx API auth: add `authInternalUsers` block to `writeConfig` granting all actions to any source IP — ironsight-api container had a non-loopback docker bridge IP, so mediamtx's default 127.0.0.1-only API ACL returned 401 on every `AddStream`/`RemoveStream` call; new cameras now register immediately without a manual mediamtx restart; port 9997 is not exposed outside the docker bridge network.



- **fix** prod-bugs camera CRUD UX: `admin/page.tsx` `ensureCameras` had a `camerasLoaded` guard that short-circuited every subsequent `onRefresh()` call, so delete/add never reflected in the list; split into `refreshCameras` (always fetches, passed as `onRefresh`) and `ensureCameras` (lazy-load guard, used only for initial tab open); `CameraManager.tsx` now awaits `onRefresh()` before closing the add modal (camera is visible the moment modal closes), surfaces success and error toasts for single delete and bulk delete, disables Cancel during active ONVIF probe, and shows a "this can take up to 60 seconds" info banner while `adding` is true; `onRefresh` prop type widened from `() => void` → `() => Promise<void>` in `CameraManager` and `SettingsPage`; backend probe is still synchronous (TODO P2: async 202+poll pattern).

- **fix** prod-bugs PR-A (frontend quick wins): suppress session-expiry banner for SSO users (ironsight_session cookie absent = oauth2-proxy session, banner is inaccurate); redirect already-authenticated users away from /login based on role; wrap AdminPage root with ToastProvider so CompanyCard/useApiAction/useToast works in all admin tabs (previously only SettingsPage was wrapped, causing "useToast must be used within ToastProvider" on Companies tab); hide docker-internal endpoints (http://yolo:8501 etc.) in ServicesHealthCard — show "internal service" instead; add degraded/stale reason tooltip to RecordingHealthCard status badge (mirrors recording_health.go threshold logic).
- **fix** Bug #4 (live feed 403): add `GET /api/auth/csrf` bootstrap endpoint (`HandleCSRF` in `auth_handler.go`, mounted before the CSRF middleware group in `router.go`) — SSO sessions never pass through `/auth/login` so they have no `ironsight_csrf` cookie; `RequireAuth` attempts to set it but `http.SetCookie` is silently dropped by the sentry/metrics/requestLogger `ResponseWriter` wrapper chain; dedicated GET endpoint is CSRF-exempt and re-uses the existing cookie value if present, eliminating the chicken-and-egg problem; `AuthContext.tsx` calls `GET /api/auth/csrf` once after `/auth/me` resolves when the cookie is absent — ensures the double-submit token exists before the first `POST /api/media/mint` (live feed) or any other non-idempotent request; branch `fix/prod-bugs-csrf-mint`.

## 2026-06-04

- **ops** OPS-01: Test/prod environment split — bob as test host. `internal/buildinfo` package (new; `GitSHA` var injectable via `go build -ldflags "-X ironsight/internal/buildinfo.GitSHA=<sha>"`); `Dockerfile` `ARG GIT_SHA=dev` + `-X ldflags` injection in all 6 binary build commands; `internal/api/router.go` `/api/health` now returns `{"status":"ok","git_sha":"<sha>"}` (empty string in local dev without ldflags); `.github/workflows/build-test-images.yml` (new; triggers on push to main, builds api/worker/frontend/qwen-test-7b/yolo, pushes to `ghcr.io/spaeny10/ironsight-*:latest` as public packages, no new secrets required); `deploy/promote-to-prod.sh` (new; ~200-line shell script, `--dry-run` flag; gates promotion on bob `/api/health → git_sha` match, checks 7-day consistency reports, retags GHCR image to `:prod-YYYY-MM-DD-<sha>`, SSHes to fred to pull+restart api+worker, appends JSON record to `/tank/data/promote-log.jsonl` on fred); `.gitignore` excludes `deploy/test/` (bob-local overlay, not committed). PR #34. Bob-local files (gitignored): `deploy/test/docker-compose.test.yml`, `deploy/test/bootstrap-bob.sh`, `deploy/test/README.md`.

## 2026-05-29

- **feat** P3-INFRA-06: gohlslib LL-HLS live view (replaces WebRTC/WHEP) — root cause: mediamtx silently drops H.265 tracks from WebRTC SDP answer (`skipping track (Generic)`), causing WHEP 400 for all HEVC cameras (3 of 4 trailer-570 cameras); new `internal/streaming/livehls.go` (`LiveHLSManager` + `LiveHLSMuxer`: lazy-start per-camera gohlslib v2 LL-HLS muxer, RTSP pull from `rtsp://<mediamtxRTSPHost>/<cameraUUID>_sub`, H.264+H.265 RTP decoders via gortsplib v5, fMP4 CMAF LL-HLS via gohlslib `MuxerVariantLowLatency` / `SegmentMinDuration=2s` / `PartMinDuration=200ms`, idle-stop after 30s at zero viewers, graceful `StopAll` on shutdown); new `auth.MediaKindLiveHLS = "live-hls"` media token kind (60-second TTL, separate audience from recorded HLS so a recording token can't trigger a live RTSP pull); `HandleMediaMint` enforces 60s TTL + synthetic path `"live"` for `live-hls` kind; `HandleMediaServe` routes `live-hls` tokens to `LiveHLSManager.GetOrStart` then `muxer.ServePlaylist`; `NewRouter` / `NewLiveHLSManager` wired in `cmd/server/main.go` with `StopAll` on graceful shutdown; `internal/streaming/mediamtx.go` WebRTC fields and WHEP proxy removed (`WebRTC: false` in `writeConfig`); frontend `VideoPlayer.tsx` WebRTC/WHEP useEffect replaced with hls.js LL-HLS path using `createMediaRefresher({ kind: 'live-hls', path: 'live' })` (30s auto-refresh, 60s TTL → token always valid at refresh time); `frontend/src/lib/media.ts` `MediaKind` union adds `'live-hls'`; `go.mod` bumps gohlslib/v2 to v2.3.2 + adds indirect deps (asticode/go-astikit v0.30.0, asticode/go-astits v1.15.0); browser compat: Safari + Chrome 107+ hw-HEVC — ✓; Firefox H.265 — ✗ (hls.js codec error, Firefox-compat banner is a future PR); `docker-compose.yml` cleanup on fred: remove `MTX_WEBRTCADDITIONALHOSTS` env + `8189:8189/udp` port publish (no longer needed); `docs/streaming.md` full architecture doc; `docs/decisions.md` P3-INFRA-06 entry.

- **fix** P3-INFRA-06 cleanup: remove diagnostic logging from `internal/streaming/livehls.go` — H264 track-found log no longer prints SPS/PPS byte lengths/payload type/packetization mode; H264 RTP callback filters `ErrNonStartingPacketAndNoPrevious` and `ErrMorePacketsNeeded` silently (matching recording-engine pattern in `gort_recorder.go`) instead of logging first-100 decode errors verbatim; spurious "hlsMuxer is now set" debug line removed from `start()`; `errors` and `rtph264` imports restored to support `errors.Is` filter pattern; `media_v1.go` synced from local canonical copy to fred (md5 divergence corrected).

## 2026-05-28

- **feat** P4-SCHEMA-06: re-analysis tooling — `internal/reanalysis/rules.go` pure rule engine (`RuleSet` struct with `confidence_threshold`, `class_remap`, `min_bbox_area`, `rules[]ClassRule`; `ApplyRuleSet` returns `OutcomeUnchanged|Changed|Dropped`; `ComputeFPRate` for ground-truth-based FP-rate calculation; fully unit-testable, no DB dependency); `internal/reanalysis/rules_test.go` (15 unit tests covering empty ruleset pass-through, confidence drop/pass, class rename, null-remap drop, unknown-class pass, min-bbox-area drop/pass, per-class rule override, empty-bbox drop, FP-rate empty/mixed/all-confirmed); `internal/database/reanalysis.go` (DB helpers: `GetModelVersion`, `UpdateAnalysisRunEnded`, `ListDetectionsForReanalysis` cursor-paginated with supersedes IS NULL filter, `ListOrgsForModelVersion`, `FetchDetectionReviewVerdicts` DISTINCT ON latest verdict, `ListModelVersionsByOrg`); `cmd/reanalyze/main.go` CLI binary (flags: `--model-version` UUID required, `--from`/`--to` RFC3339 required, `--organization-id` optional org scope, `--dry-run` no-insert mode, `--report-out` path, `--report-dir` directory; inserts one `analysis_runs` row with `run_type='reanalysis'`; iterates live detections via cursor pagination; calls `InsertDetection` with `supersedes=<old_id>`, `source='reanalysis'`; stamps `ended_at`; produces JSON comparison report with `totals{old_model_detections,new_model_detections,delta,delta_pct}`, `by_class_change[]`, `false_positive_rate{old_model,new_model,delta,ground_truth_samples}` omitted when no reviews exist, `duration_ms`); `internal/api/reanalyze_handler.go` (admin async endpoint `POST /api/admin/reanalyze` → 202+run_id, `GET /api/admin/reanalyze/{run_id}` poll; `HandleListDetections` GET /api/v1/detections with optional `?model_version_id=` filter defaulting to latest-by-deployed_at; `HandleListModelVersions` GET /api/v1/model-versions for frontend dropdown; `runReanalysis` shared logic); `internal/api/router.go` wires all new routes; `internal/reanalysis/integration_test.go` (4 integration tests: supersede count + append-only invariant, detections_current hides superseded, cross-tenant isolation, append-only trigger still enforced post-reanalysis); `Dockerfile` adds `/app/reanalyze` binary; `docs/reanalysis.md` ops runbook (scope cap, model_version insert, CLI usage, report interpretation, rollback strategy, RLS note, admin API, detection filter); RLS: worker connects as onvif (service_bypass) — no AcquireWithTenant; "latest model" semantic = newest deployed_at across all domains; no new migration needed; branch `p4-schema-06-reanalysis`.

- **feat** P4-SCHEMA-07: Postgres Row-Level Security defense-in-depth — migration 0031 (`app_current_tenant()` SQL function reading `current_setting('app.current_tenant', true)`; RLS ENABLE + FORCE on 19 customer-data tables; `tenant_isolation` PERMISSIVE policy USING/WITH CHECK on `organization_id = app_current_tenant()` per table, special-cased for `organizations` (`id = app_current_tenant()`) and `digest_sends` (`org_id = ...`); `service_bypass` PERMISSIVE policy TO onvif, postgres USING (true) for migration/worker/DBA paths; TimescaleDB hypertables `detections`, `person_track_frames` — ALTER TABLE against hypertable propagates to chunks per documented pattern; tables excluded: cameras/segments/events/vca_rules (no org_id), audit_log (no org_id), goose/system/alarm-queue tables); `internal/database/rls.go` (`AcquireWithTenant(ctx, pool, tenant) → (conn, tx, err)` acquires connection, begins explicit transaction, `SET LOCAL app.current_tenant = $1` — LOCAL scopes GUC to transaction so automatic RESET on rollback/commit; `SetTenantOnConn` lower-level helper for worker per-org iteration); `internal/api/rls_middleware.go` (`RLSMiddleware` reads `claims.OrganizationID` from context post-RequireAuth, stores in `contextKeyTenant{}` for handlers to retrieve via `TenantFromContext`); `internal/api/router.go` wires `RLSMiddleware` inside the `/api` auth group; `internal/database/rls_test.go` (6 integration tests: cross-tenant reads return 0 rows, WITH CHECK blocks cross-tenant INSERT, service-bypass role sees all, GUC reset on release, null-tenant docs test, sites isolation, organizations isolation); `docs/security/rls.md` (table coverage, GUC contract, service-mode worker pattern, debugging "can't see rows" guide); `docs/decisions.md` new file (Option A vs Option B rationale, RLS pointer, TEXT PK and append-only notes); branch `p4-schema-07-rls`.

- **feat** P4-SCHEMA-02: parallel-write to detections from legacy event/alarm/VLM paths — new `internal/dualwrite/` package: `Write()` (non-fatal goroutine-safe dual-write), `LookupOrCreateModelVersion()` (in-process cache keyed by org×name×tag, INSERT-or-SELECT on conflict), `NewRunHandle()` (lazy analysis_run materialization), `NormalisePPEClass()` / `NormaliseEventTypeToSecurityClass()` / `BBoxFromX1Y1X2Y2()` mappers; Prometheus counter `ironsight_detections_dual_write_failures_total{source}` registered in `init()`, added to DualWriteFailuresTotal export; four legacy sources wired: (1) PPE worker `persistViolation` → `go dualWritePPEViolation()` after `InsertPPEQueueEntry` succeeds, DetectionDomain="ppe"; (2) ONVIF event subscriber in `cmd/server/main.go` → `go dualWriteSecurityEvent()` after `db.InsertEvent()`, DetectionDomain="security"; (3) Sense webhook (`internal/api/sense_webhook.go`) → goroutine `dualWriteSenseEvent()` after `db.InsertEvent()`, model "sense-pir v1", Confidence=1.0; (4) alarm AI PPE violations in server main → `go dualWriteAlarmPPEViolations()` after `UpdateAlarmAI()` when len(PPEViolations)>0, one detections row per violation; VLM worker (`internal/safety/vlm_worker.go`) → `go dualWriteVLMVerdict()` after `UpdateVLMVerdict()`, DetectionDomain="vlm_validation"; all goroutines use background context with 10 s deadline; legacy table remains authoritative — dual-write failures are non-fatal (log+counter only, no legacy rollback); `internal/database/vlm_queue.go` VLMQueueRow extended with OrganizationID/CameraID/SiteID + ListPendingVLM query updated accordingly; new `cmd/consistency-check/main.go` binary (flags: --days 7, --out; env: DATABASE_URL, REPORT_DIR): per-org per-UTC-day window counts legacy events + alarm PPE violations + PPE queue rows vs detections by domain, marks "settled" windows (end > 5 min before check time), flags settled windows with non-zero divergence, writes JSON to `/opt/ironsight-consistency-reports/YYYY-MM-DD.json`, exits 1 on any flagged window; `deploy/systemd/ironsight-consistency-check.{service,timer}` (daily 04:00 UTC, RandomizedDelaySec=2min, Persistent=true, User=jetstream); `docs/INFRA_MASTER.md` fred timer table updated; 7 integration tests in `internal/dualwrite/dualwrite_test.go` (PPE violation, security event, VLM verdict, alarm multi-bbox, model-version cache idempotency, cancelled-context non-fatal, consistency binary end-to-end); person_track_frames explicitly NOT dual-written (not a "detection" in the D-07 sense); branch `p4-schema-02-parallel-write`.

## 2026-05-27 (continued — P4-SCHEMA-01)

- **feat** P4-SCHEMA-01: detections foundation tables — migration 0030 (`-- +goose NO TRANSACTION`): `model_versions` (org-scoped UUID PK, organization_id TEXT, model_name, version_tag, weights_hash, model_domain CHECK, deployed_at, retired_at, params JSONB, created_at; UNIQUE idx on org×name×tag); `analysis_runs` (UUID PK, org FK RESTRICT, model_version_id FK RESTRICT, run_type CHECK live_ingest|reanalysis, started_at, ended_at, params JSONB, created_at); `detections` (TimescaleDB hypertable on detected_at, 7-day chunks; UUID id, org/site/camera FKs, detection_class, detection_domain CHECK, confidence REAL, bounding_box JSONB single-object DECISION-C, zone_id→ppe_zones + vca_rule_id→vca_rules as mutually-exclusive nullable FKs DECISION-B, model_version_id + analysis_run_id FKs RESTRICT, segment_id BIGINT SET NULL, frame_offset_ms, source CHECK live|reanalysis, supersedes UUID self-FK, details JSONB, created_at; append-only via existing ironsight_prevent_mutation() trigger); `detection_reviews` (UUID PK, org FK RESTRICT, detection_id FK RESTRICT, reviewer_user_id FK RESTRICT, verdict CHECK confirmed|false_positive|uncertain, notes TEXT, reviewed_at; DECISION-D2: corrections live here not as detection rows); `detections_current` VIEW (NOT EXISTS supersedes subquery + idx_det_supersedes); 6 indices on detections + 2 on detection_reviews + 2 on model_versions + 2 on analysis_runs; down section drops in reverse, no remove_retention_policy call; `internal/database/detections.go` (ModelVersion, AnalysisRun, Detection, DetectionReview structs + Insert variants; InsertModelVersion, InsertAnalysisRun, InsertDetection, GetDetection, ListDetectionsCurrent, InsertDetectionReview — append-only respected, no UPDATE/DELETE methods, tenant scope on every query); `internal/database/detections_test.go` (5 integration tests: insert round-trip, append-only trigger rejects UPDATE+DELETE, supersede chain→detections_current excludes superseded, detection_reviews FK integrity, cross-tenant isolation); `id_convention_test.go` updated (5 new TEXT *_id FK columns for P4 tables added to textIDColumnAllowlist); foundation-only — P4-SCHEMA-02 wires parallel-write next.

## 2026-05-27 (continued)

- **feat** P3-INFRA-08: weekly PPE compliance digest email — migration 0029 (`digest_sends` table: org_id TEXT, scope TEXT, period_start TIMESTAMPTZ, sent_at TIMESTAMPTZ, UNIQUE(org_id, scope, period_start) durable idempotency key); `internal/database/digest_sends.go` (`GetLastDigestSend`, `UpsertDigestSend`); `MatchWeeklyDigestRecipients` added to `internal/database/notifications.go` (mirrors `MatchMonthlySummaryRecipients`, filters `event_type='weekly_digest'`); `WeeklyDigestContext` + `DigestTopCamera` structs + `WeeklyDigest` Dispatcher method added to `internal/notify/dispatcher.go` (table-based HTML layout: `<table>/<tr>/<td>` throughout, NO `display:flex` or `statTile`, inline CSS, Outlook-safe; multipart text+html with compliance rate, violations, pending count, top cameras, person-hours); `internal/config/config.go` adds `DigestSendDay`/`DigestSendHour`/`DigestScope`/`DigestNoActivitySkip` with `clampWeekday`/`clampHour`/`digestScopeFromEnv` helpers; `cmd/worker/main.go` adds `runWeeklyDigest` goroutine (30-min poll, leader-elected, checks `digest_sends` before each org send, per-org error isolation, 200ms rate-limit between sends) + `runWeeklyDigestForOrg` (queries `GetComplianceHeadline`/`GetComplianceTopCameras`/`GetComplianceOccupancy`, no-activity skip, soft-delete-safe via `ListSitesScoped` which reads `sites_active`); stub-mode default (no SMTP configured → `[NOTIFY-STUB]` logs); CAN-SPAM note: unsubscribe via login-gated `/portal/notifications` (tokenized one-click unsubscribe is a pre-launch follow-up); tests: `internal/notify/dispatcher_weekly_digest_test.go` (HTML content, no-flex safety, multipart, stub mode, empty recipients, send-error isolation) + `internal/database/digest_sends_test.go` (idempotency, scope isolation, different-weeks, MatchWeeklyDigestRecipients filter, cross-tenant isolation, no-activity skip, soft-delete exclusion); branch `feat/p3-infra-08-weekly-digest`.

## 2026-05-27

- **feat** P3-INFRA-05: soft-delete pattern — migration 0028 adds `deleted_at TIMESTAMPTZ` to 8 tables (cameras, sites, organizations, users, speakers, ppe_zones, compliance_rules, vca_rules); `_active` views per table (`SELECT * WHERE deleted_at IS NULL`); partial unique indexes free soft-deleted rows' unique slots (cameras.sense_webhook_token, users.username); `SoftDeleteCamera/Speaker/Site/Organization/User/PPEZone/ComplianceRule/VCARule` DB functions with cascade semantics (camera→ppe_zones+compliance_rules+vca_rules; site→cameras→children; org→sites sequential; ppe_zone→compliance_rules); Trap 1 fix: ListSites/ListSitesScoped camera count JOIN filters `c.deleted_at IS NULL`; Trap 2 fix: `GetOrCreateUserByEmail` resurrects soft-deleted users before INSERT to prevent partial unique index violation on SSO re-login; all API DELETE handlers updated to call SoftDeleteX; admin-only `?include_deleted=true` parameter on all list endpoints (cameras, sites, organizations, users, speakers, ppe-zones, compliance-rules); `HandleDeletePPEZone` 409 guard removed (soft-delete cascades rules safely); `internal/database/soft_delete_convention_test.go` CI lint (8 required tables have deleted_at, 12 excluded tables do not, 8 _active views exist); `docs/soft-delete.md`; branch `feat/p3-infra-05-soft-delete`.

- **feat** P3-INFRA-04 (Interpretation B): ID standardization completion — migration 0027 (`vlm_label_jobs.camera_id` TEXT NOT NULL DEFAULT ''→UUID nullable + FK to cameras(id) ON DELETE SET NULL; pre-flight: 5 rows all valid UUID strings; migration: dropped NOT NULL + DEFAULT, NULLed 5 orphaned rows (cameras deleted pre-launch), USING cast, added FK; goose v27 applied 443ms); `VLMLabelJob.CameraID` field updated to `*uuid.UUID` (nullable); `EnqueueLabelJob` cameraID param is `uuid.UUID` (non-nil at enqueue time); goroutine call site in `cmd/server/main.go` passes `cameraID uuid.UUID` directly; `internal/database/id_convention_test.go::TestSchemaIDConventions` CI lint — asserts no new TEXT-PK tables outside grandfathered allowlist (organizations/sites/incidents/active_alarms/security_events/site_sops/company_users/operators/revoked_tokens/evidence_shares) and no TEXT *_id columns in UUID-convention tables; `backend-integration` CI job added to `.github/workflows/ci.yml` (TimescaleDB service + DATABASE_URL + IRONSIGHT_INTEGRATION_REQUIRED=1); `docs/id-conventions.md` documents the canonical two-tier policy and grandfathered TEXT-PK rationale; `docs/migrations.md` cross-links id-conventions.md; TEXT PKs on org/site/SOC tables NOT converted (Interpretation A rejected — slug format confirmed on fred: co-bv-test, T5-903); image sha256:f0c3d14170c05372f9121b94ebe18e6a82150a718d97d6acb063215ca774ce6e; commits 92857fa + 493cff6; branch `feat/p3-infra-04-id-standardization`.

- **feat** P3-INFRA-03: evidence chain-of-custody manifests — migration 0026 (`evidence_manifests` table: uuid PK, artifact_type CHECK, organization_id, artifact_sha256, ed25519 signature, key_id, sig_algorithm, parent_manifest_id, manifest_json JSONB; append-only trigger via `ironsight_prevent_mutation()`; indices on (org, created_at DESC) + (artifact_type, artifact_id)); `internal/evidence/manifest.go` (`BuildManifest` deterministic canonical JSON, `SignManifest` ed25519, `VerifyManifest` keyring-lookup, `ParseED25519PrivateKeyHex`, `ParseED25519PublicKeyHex`, `ED25519PublicKeyFingerprint`); `internal/database/evidence_manifests.go` (append-only: `InsertManifest`, `GetManifest` tenant-scoped, `ListManifests` tenant-scoped; no UPDATE/DELETE methods); `internal/api/evidence_manifest_handler.go` (`HandleGetManifest`, `HandleListManifests`, `HandleVerifyManifest` + `buildKeyringFromConfig`); `internal/api/evidence_manifest_write.go` (`writeManifest` best-effort helper, `SHA256Hex`); `cmd/verify-manifest/main.go` (offline CLI: takes manifest row JSON + pubkey hex, exits 0/1); `HandleComplianceReportPDF` now accepts `cfg *config.Config`, writes `compliance_report` manifest after PDF rendered (goroutine, context.Background); `evidence_export.go` writes `clip_export` manifest after sw.Sign, artifact_sha256=SHA-256(event.json bytes); 3 routes added to router.go (`GET /api/evidence/manifests`, `/{id}`, `/{id}/verify`); config adds `EvidenceED25519PrivateKey` + `EvidenceSigningKeyring`; Dockerfile adds 5th binary `verify-manifest`; `docs/chain-of-custody.md` (manifest format, key management, rotation, offline verification, append-only guarantee, cross-tenant isolation); `docs/configuration.md` updated; ed25519 via stdlib `crypto/ed25519` — no new deps; existing HMAC SignedZipWriter preserved additive; 12 unit tests (determinism, sign/verify round-trip, tamper-artifact-sha256, tamper-signature, wrong-key, rotation, missing-key-id, key-not-in-keyring, parse-hex-seed, parse-hex-full-key); branch `feat/p3-infra-03-chain-of-custody`.



- **feat** P2-C-05: ROI cropping for VLM input — new `internal/safety/roi_crop.go` (`CropToROI` pure function: Go stdlib `image`/`image/jpeg` only, no new deps); normalized bbox → pixel coords with padding (`VLM_CROP_PADDING_FACTOR`, default 0.25), clamped to frame bounds, inverted-bbox swap, degenerate guard returns `ErrDegenerateBBox`; `internal/safety/vlm_worker.go` inserts crop step in `processRow` before the Qwen call with full-frame fallback on crop error (graceful degradation — validation always proceeds); `buildPPEPrompt` in `vlm_validator.go` strips frame-relative bbox coordinates from prompt text (crop is already the focused region); `VLMCropPaddingFactor float64` added to `internal/config/config.go`; `docs/configuration.md` adds PPE/VLM worker section including `VLM_CROP_PADDING_FACTOR`; 49 total safety tests pass (15 pre-existing engine/polygon + 9 validator + 14 worker [6 original + 8 new crop/integration] + 11 CropToROI); go vet + 4 builds clean on fred; worker image sha256:21ce9b34d5c18605b718616732b311b33290268e357ad99e1b41562b2860f6c3; commits `b8a72af` + `4321944`; branch `feat/p2-c-05-roi-cropping`.

- **feat** P2-C-03: async VLM validation worker for PPE candidates — migration 0025 renames C-01 stub VLM columns to richer verdict model (`vlm_verdict TEXT CHECK`, `vlm_reasoning`, `vlm_checked_at`, `vlm_model`, `vlm_attempts INT`); new `internal/safety/vlm_validator.go` (`ValidatePPECandidate`, `AIClient` interface, verdict mapping with embedded-JSON parse fallback, conservative thresholds: >=0.65 dismissed / <=0.35 confirmed); new `internal/safety/vlm_worker.go` (async poll loop, per-row `recover()`, stale-age-out via `ExpireStalePendingVLM`); new `internal/database/vlm_queue.go` (`ListPendingVLM`, `UpdateVLMVerdict`, `ExpireStalePendingVLM`); `internal/ai/client.go` gets `PPEValidationResult` struct + `ValidatePPEFrame` public wrapper around existing `analyzeQwen`; `internal/api/ppe_queue_handler.go` adds VLM fields to response + `?include_dismissed=true` param; worker gated by `VLM_WORKER_ENABLED=false` (default) — Qwen not running on fred, candidates stay at `vlm_verdict='pending'`; `[TRACKING]` + `[PPE]` workers regression-free; 15 new tests (validator + worker); 30 total safety tests pass; goose v25 on fred; image sha256:850b640fcd9adba6cc49ded8e09bb645b1be36dfa2c96e5d0740bf9d432210c2; commit `44df6a3`. branch `feat/p2-c-03-vlm-validation`.

- **feat** P1-A-02 PR3: retire Authorization: Bearer session path + switch /metrics to network-trust — `RequireAuth` in `internal/api/auth_handler.go` now accepts ONLY `ironsight_session` HttpOnly cookie (or X-Forwarded-Email SSO header); the `Authorization: Bearer <sessionJWT>` branch has been removed; `METRICS_AUTH` default changed from `sso` to `none` in `internal/config/config.go` + `metricsAuthFromEnv()`; `/metrics` route in `internal/api/router.go` is no longer wrapped in `RequireAuth` when `METRICS_AUTH=none`; `docs/metrics.md` + `docs/configuration.md` updated with NPM network-restriction requirement; `TestRequireAuth_AuthHeaderRejected` + `TestRequireAuth_CookieStillAccepted` + `TestRequireAuth_SSOStillWorks` + `TestMetrics_NetworkTrust` in `internal/api/auth_header_retire_test.go`; `TestRequireAuth_AuthHeaderStillAccepted` removed from `cookie_csrf_test.go`; frontend `getStoredToken` no-op export + dead `api.ts` import cleaned up.

## 2026-05-26

- **feat** P2-C-06: compliance dashboard + PDF reports — `internal/database/compliance_queries.go` (5 tenant-scoped aggregation functions + `VerifySiteOwnership`); `internal/pdf/` package (`embed.go` + Liberation Sans go:embed, `report.go` A4 PDF via signintech/gopdf v0.27.0 with 6 sections); `internal/api/compliance_handler.go` (`HandleComplianceSummary` + `HandleComplianceReportPDF`); routes `GET /api/v1/portal/compliance/summary` and `GET /api/v1/portal/compliance/report.pdf`; Dockerfile updated to apt-install fonts-liberation + go mod tidy before go build; frontend: `app/portal/compliance/page.tsx`, 4 portal components (ComplianceSummaryCards, ComplianceViolationsChart, ComplianceCamerasTable, ComplianceFindingsTable), `compliance.css` (warm-light tokens only); types added to `ironsight.ts` (PortalSummary, PPEZone region field, ComplianceRule/Create enabled+site_wide+notes); import/export pattern fixed in api.ts; cross-tenant 403 guard on all site_id paths; graceful degradation when person_track_buckets absent; API image sha256:866b164191ec, frontend image sha256:6417bfe4169877e; both endpoints verified on fred — summary 200 + PDF %PDF magic bytes + X-Report-Id UUID header.

- **feat** P2-C-02: person-tracking aggregation worker — migration 0023 (`person_track_frames` TimescaleDB hypertable + `person_track_buckets` regular table, `NO TRANSACTION` for retention policy); `internal/tracking` package (Worker + Aggregator with 5-min bucket roll-up and 24h startup backfill); typed tenant-scoped DB layer (`tracking.go`); `GET /api/v1/portal/person-tracks` handler with allowedBucketMinutes={5,15,60}; PPE worker fan-out via non-blocking `TrackingCh`; R1 guard (`countPersons` reads `YOLOResult.Detections` only, never `PPEViolations`); retention sweep extended to both tracking tables; 14 unit tests pass; migration 0023 applied on fred; all containers restarted cleanly.

- **feat** P2-C-01: PPE detection pipeline — migrations 0021 (sites.ppe_enabled) + 0022 (pending_review_queue); internal/ppe/worker.go polls PPE-enabled cameras every 30s (Milesight CGI → ONVIF fallback), calls YOLO sidecar, filters by confidence threshold (default 0.50), writes violation JPEG to /tank/data/ironsight/ppe-frames, inserts to pending_review_queue, broadcasts ppe_detected WS envelope with top-level camera_id; internal/database/ppe_queue.go provides Insert/List/Get/Update/Sweep CRUD with string org/site IDs matching existing TEXT PKs; 3 API routes (GET list, POST review, GET frame); frontend PendingReviewQueue.tsx replaces MOCK_FINDINGS with real fetch + WS; 9 tests pass; containers restarted cleanly; endpoint 401-gates correctly.

- **feat** P1-A-04: WebSocket auth + tenant-scoped fanout — `HandleWebSocket` now calls `resolveWSAuth` (checks `?ticket=<wsTicket>` or `X-Forwarded-Email`; rejects legacy `?token=`) BEFORE `upgrader.Upgrade` so unauthenticated upgrades return 401; per-conn `wsClient` carries claims + `allowedCameras` set from `AuthorizedCameraIDs`; `writeToClients` filters via `routeKeyFromMessage` + `canSee` (top-level OR `data.camera_id` routing key; default-deny for restricted users on uuid.Nil system messages); `runRBACRefresher` goroutine launched in `Hub.Run` at 60 s interval via `Hub.Configure(cfg, db)` from `main.go` — DB error keeps cached set, globalView clients skipped; `hub.Configure(cfg, db)` replaces `SetRBACSource` for wiring; `frontend/src/app/page.tsx` updated to fetch `?ticket=` before opening `ws://...` (matching ws-alerts.ts pattern already shipped in P1-A-02); 12 new tests in `internal/api/websocket_test.go` (all pass); curl confirms 401 on unauthenticated `/ws`; RBAC refresher log line `[WS] RBAC refresher started (interval=1m0s)` confirmed on fred.

- **feat** P1-C-04: alertmanager + ntfy alerting — new `deploy/monitoring/` directory with `prometheus.yml` (scrape config with bearer-token auth), `alerts.yml` (12 rules: `IronsightAPIDown`, `IronsightHighErrorRate`, `IronsightSlowRequests`, `IronsightRecordingStalled`, `IronsightCameraDropoff`, `IronsightFFmpegMismatch`, `IronsightDBPoolExhausted`, `IronsightWSClientsLost`, `IronsightRBACRefreshErrors`, `IronsightMigrationBehind`, `IronsightAppAlert`, `IronsightAppAlertWarning`), `alertmanager.yml` (ntfy webhook receiver with critical/warning routing + inhibition), `ntfy-template.tpl` (Go template for phone notifications), `DEPLOY.md` (LXC provisioning guide), `README.md`; new `internal/metrics/custom_alert.go` with `SetCustomAlert()` helper + `ironsight_app_alert_total{name,severity}` counter wired at 3 call sites (goose migration failure in `cmd/server/main.go`, FFmpeg crash in `internal/recording/engine.go`, credential encrypt failure in `cmd/server/camera_creds_sweep.go`); 5 new tests in `internal/metrics/custom_alert_test.go`; new `docs/alerting.md` alert catalog; new `docs/runbooks/` with README + 8 runbook stubs.
- **feat** P1-C-02: Sentry/GlitchTip error reporting — new `internal/logging/sentry_handler.go` `SentryHandler` wraps the P1-C-01 slog handler; Error-level records captured to Sentry with structured attrs in `Contexts["log_attrs"]`, sensitive keys redacted (`password|secret|token|key|credential|authorization|auth`) at classify time + `BeforeSend` hook; `request_id`/`route`/`method`/`status`/`path` promoted to Sentry tags; `sentryhttp` panic middleware as outermost router layer; `InitSentry`/`FlushSentry` in both `cmd/server` + `cmd/worker` via `cfg.SentryDSN`; 14 tests pass; `SENTRY_DSN=""` in fred `.env` (no-op until GlitchTip LXC); deployed to fred.
- **feat** P1-C-03: Prometheus `/metrics` endpoint — new `internal/metrics/` package with 12 named series (`ironsight_http_requests_total`, `ironsight_http_request_duration_seconds`, `ironsight_recording_active_cameras`, `ironsight_recording_ffmpeg_subprocesses`, `ironsight_recording_segments_written_total`, `ironsight_db_pool_acquire_count/idle/total`, `ironsight_ws_clients_connected`, `ironsight_rbac_cache_refresh_total/errors`, `ironsight_goose_migration_version`); non-default registry; route-pattern labels for bounded cardinality; SSO-gated by default (`METRICS_AUTH=sso`); disable via `METRICS_ENABLED=false`; DB pool stats synced every 15 s; 7 unit tests; deployed to fred.
